Notes:
- Image were moved in a (temporarly) private repository

toThink:
- JS injection (and not per se' HTML reading) for background-image exception: https://css-tricks.com/apply-a-filter-to-a-background-image/ => bad idea for normal backgrounds (but you can distinguish them)
